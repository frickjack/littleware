package littleware.base.event;

/** 
 * Interface for classes that notify listeners for both
 * PropertyChange event and LittleEvents
 */
public interface LittleTool extends LittleBean, LittleEventSource {
}
